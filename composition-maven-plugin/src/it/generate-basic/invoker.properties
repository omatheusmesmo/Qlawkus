# Run the goal through its default lifecycle binding (generate-sources), so this exercises the
# execution wiring an app pom uses, not just a direct goal invocation. The reactor spans the two
# capability modules and the agent app, so ${reactorProjects} is populated for real.
invoker.goals = generate-sources
invoker.buildResult = success
