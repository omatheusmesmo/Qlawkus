# The manifest lists an 'except' entry that no reactor module declares, so the default fail-fast
# validation must break the build. Proves the anti-typo guard end to end.
invoker.goals = generate-sources
invoker.buildResult = failure
