# Build-time config overrides, empty by default. This file is owned by the promote step
# (examples/redeploy/redeploy.sh) - it overwrites this file wholesale from the staged overrides
# (GET /api/admin/config-overrides) before each rebuild. Do not hand-edit; use the console config
# editor or the admin API instead.
